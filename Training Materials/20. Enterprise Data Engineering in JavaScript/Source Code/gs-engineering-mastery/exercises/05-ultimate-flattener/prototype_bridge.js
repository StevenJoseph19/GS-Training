/**
 * LAB R-05: Prototype Bridge
 * Goal: Create a base "Instrument" prototype that provides
 * shared valuation and risk methods to all asset instances.
 */

const Instrument = {
  /**
   * Shared Valuation Method
   * Note: This uses 'this', which requires .apply() in our memoizer
   * to ensure it points to the specific asset (e.g., GS or AAPL).
   */
  /**
   * Shared Formatting Method
   */
  /**
   * Shared Validation
   */
};

module.exports = { Instrument };
