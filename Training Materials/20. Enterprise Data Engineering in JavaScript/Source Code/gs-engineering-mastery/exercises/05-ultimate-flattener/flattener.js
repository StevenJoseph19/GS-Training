/**
 * LAB R-01: In-Place Flattener
 * Goal: Convert a nested tree (Desks -> Portfolios -> Assets) into a flat Map.
 * This allows the TradingEngine to access any asset in O(1) time.
 */
function flatten(data, result = {}) {}

module.exports = { flatten };
