
'use client';
// TODO: Import CarRow and LoadingIndicator when implemented
// TODO: Import { useCars } from '@/hooks/useCars'
// TODO: Import type { Car } from '@/lib/types'

/**
 * CarList (ASSIGNMENT SKELETON)
 * -----------------------------
 * TODOs:
 * 1) Get `{ cars, status }` from `useCars()`.
 * 2) If status !== 'loaded', return <LoadingIndicator status={status} />.
 * 3) Otherwise render a table with headers and map cars to <CarRow/>.
 * 4) CarRow should trigger navigation to details via context (handled in CarRow).
 */
export default function CarList() {
  // TODO: Implement loading and table rendering per above
  return null;
}
