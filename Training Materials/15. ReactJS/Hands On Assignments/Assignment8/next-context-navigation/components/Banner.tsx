
'use client';
import { useNavigation } from '@/contexts/navigation';

/**
 * Banner (ASSIGNMENT SKELETON)
 * ----------------------------
 * TODOs:
 * 1) Render a semantic <header> with an <h1> title and optional subtitle.
 * 2) Add a "Home" button with aria-label="home" that calls navigate('home').
 * 3) Keep styles simple (inline or CSS) – trainees choose.
 */
export default function Banner() {
  const { navigate } = useNavigation();
  // TODO: Implement header, title, subtitle, and Home button
  return null;
}
