
'use client';
// TODO: Import Image from 'next/image'
// TODO: Import { defaultPhoto } from '@/helpers/defaultPhoto'
// TODO: Import { useNavigation } from '@/contexts/navigation'

/**
 * CarDetails (ASSIGNMENT SKELETON)
 * --------------------------------
 * TODOs:
 * 1) Read `param` from `useNavigation()` and treat it as the selected car.
 * 2) Compute `src`: if car.photo truthy -> `/carImages/${car.photo}` else `defaultPhoto`.
 * 3) Render a Back button with aria-label="back" that calls navigate('home').
 * 4) Render a heading with brand + model, a price paragraph, and the <Image>.
 */
export default function CarDetails() {
  // TODO: Implement details UI, image, and Back button
  return null;
}
