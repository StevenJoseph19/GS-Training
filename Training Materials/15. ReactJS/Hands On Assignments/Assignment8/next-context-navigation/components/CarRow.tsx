
'use client';
// TODO: Import type { Car } from '@/lib/types'
// TODO: Import { useNavigation } from '@/contexts/navigation'

/**
 * CarRow (ASSIGNMENT SKELETON)
 * ----------------------------
 * TODOs:
 * 1) Render a <tr> with cells for brand, model, price.
 * 2) Conditionally style price (> 30000) with a highlight style.
 * 3) On row click, call navigate('details', car).
 * 4) Add data-testid={`car-row-${car.id}`}.
 */
export default function CarRow(/* props: { car: Car } */) {
  // TODO: Implement row UI and click handler
  return null;
}
