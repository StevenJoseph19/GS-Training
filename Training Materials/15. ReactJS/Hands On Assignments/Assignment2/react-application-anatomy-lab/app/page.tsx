
'use client';
import React, { useState } from 'react';
// TODO: import components via the barrel file './components'

export default function Page() {
  // TODO: keep active tab in state; default 'dashboard'
  // const [tab, setTab] = useState<'dashboard' | 'houses'>('dashboard');

  return (
    <main>
      {/* TODO: Banner with title "Globomantics Portal" */}
      {/* TODO: NavTabs with active + onChange */}

      {/* TODO: conditional render Dashboard (Stats + Chart) or Houses list */}
      <section data-testid={/* 'dashboard' or 'houses' container */ undefined}>
        {/* TODO */}
      </section>
    </main>
  );
}
