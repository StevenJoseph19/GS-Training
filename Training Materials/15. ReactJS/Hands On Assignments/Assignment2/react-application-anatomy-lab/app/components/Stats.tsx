
'use client';
import React from 'react';
// TODO: import default and named exports from '@/lib/utils/math'
import { STOCK } from '@/lib/data/houses';

// TODO: Compute total and average prices using utils and render with data-testids
// - section data-testid="stats"
// - stat-total (count), stat-sum (formatted), stat-avg (formatted)
export function Stats() {
  return (
    <section data-testid="stats">
      <h2>Inventory Stats</h2>
      <p>TODO stats</p>
    </section>
  );
}
