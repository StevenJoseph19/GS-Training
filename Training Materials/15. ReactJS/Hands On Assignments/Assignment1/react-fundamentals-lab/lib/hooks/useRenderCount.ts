
import { useRef } from 'react';

/**
 * useRenderCount
 * Tracks how many times the component has rendered.
 * Returns 1 on initial render, incrementing each render.
 */
export function useRenderCount(): number {
  // TODO: Implement using a ref. Increment on each render.
  // Hint:
  //   const ref = useRef(0);
  //   ref.current += 1;
  //   return ref.current;
  return 0; // TODO replace
}
