
'use client';
import React from 'react';
import { Banner } from './components/Banner';
import { HouseList } from './components/HouseList';

// TODO: Render two banners and the house list:
// - First Banner uses headerText containing "Globomantics"
// - Second Banner renders children: "Alternate Title via Children"
// - Then render <HouseList />
export default function Page() {
  return (
    <main>
      {/* TODO: first Banner via headerText */}
      {/* TODO: second Banner via children */}
      {/* TODO: HouseList */}
    </main>
  );
}
