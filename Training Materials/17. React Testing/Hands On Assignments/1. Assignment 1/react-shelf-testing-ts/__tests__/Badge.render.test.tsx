/**
 * Problem Statement: Badge Component — Rendering
 *
 * The `Badge` component should display the current count inside a status element
 * that is accessible to assistive technologies.
 *
 * Your tasks:
 *   1) Render the component with a sample count.
 *   2) Locate the status element in an accessibility-friendly way.
 *   3) Verify that the displayed text reflects the provided count.
 *
 * Guidelines:
 *   - Use user-centric queries and readable assertions.
 *   - Keep the test resilient to markup changes (avoid brittle selectors).
 *   - Focus on what the user sees, not on implementation details.
 *
 * Notes:
 *   - Component under test: `src/components/Badge.tsx`
 *   - Uncomment imports below when you implement the tests.
 */

import { describe, it } from "vitest";
// import React from 'react';
// import { render, screen } from '@testing-library/react';
// import Badge from '../src/components/Badge.tsx';

describe("Badge rendering (TS)", () => {
  it("renders the provided count in an accessible status element", () => {
    // TODO: Arrange — set up the component under test with a chosen count value.
    // TODO: Act — obtain a reference to the status element using an accessibility-first query.
    // TODO: Assert — verify the text communicates the correct count to the user.
  });
});
