import React from "react";
import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";
// import SiteTitle from '../src/components/SiteTitle';

describe("SiteTitle", () => {
  it("renders the site name as a heading", () => {
    // TODO: Arrange — Define the site name (e.g., "World Recipes") and render the SiteTitle component, passing the name prop.
    // TODO: Act — Query for the heading element by its role ('heading') and the text content matching the site name.
    // TODO: Assert — Verify that the heading element is present in the document.
  });
});
