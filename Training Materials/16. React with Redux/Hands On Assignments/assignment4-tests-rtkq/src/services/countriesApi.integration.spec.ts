import { describe, it } from 'vitest';
// Assignment 4 — Integration test (STUB)
// TODO: Use MSW to mock REST Countries API and test RTK Query store integration:
// - setupServer + rest.get handler
// - configureStore with api.reducer + middleware
// - dispatch getCountries.initiate() and assert fulfilled and data shape

describe('RTK Query integration with store (STUB)', () => {
  it.todo('getCountries fulfills and returns data (mocked)');
});
