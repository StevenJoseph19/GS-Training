import { describe, it } from 'vitest';
// Assignment 4 — Selector tests (STUB)
// TODO: Write tests for filterCountries with scenarios:
// - undefined list => []
// - empty query + All region => all
// - case-insensitive query
// - region-only
// - intersection
// - whitespace query treated as empty

describe('filterCountries(list, filters) (STUB)', () => {
  it.todo('undefined list => []');
  it.todo('empty query + region=All returns all items');
  it.todo('case-insensitive query by name.common');
  it.todo('region-only filter');
  it.todo('query + region intersection');
  it.todo('whitespace query treated as empty');
});
