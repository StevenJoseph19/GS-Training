// Topic: Spread with Objects (immutable updates)
// Concept: Object spread is shallow; spread nested objects too.
// Problem: Add `position` to `user.organization` immutably.
// Hints: Nested spread.
// Examples:
// Input: updatePosition(user,'Director')
// Output: organization keeps fields + position


function updatePosition(user, position) {
  // TODO: implement
}

// Test cases (uncomment to run after implementation)
// // console.log(updatePosition({ id:3, name:'Ron', organization:{ name:'P&R', city:'Pawnee'} }, "Director"));
