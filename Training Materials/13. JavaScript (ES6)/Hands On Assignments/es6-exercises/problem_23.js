// Topic: Promises (resolve)
// Concept: A Promise fulfills; `.then` handles success; `.finally` runs after settlement.
// Problem: Create a promise that resolves after delay.
// Hints: Use `setTimeout(resolve, ...)`.
// Examples:
// Input: demoPromiseResolve()
// Output: logs then and finally


function demoPromiseResolve() {
  // TODO: implement
}

// Test cases (uncomment to run after implementation)
// // demoPromiseResolve();
