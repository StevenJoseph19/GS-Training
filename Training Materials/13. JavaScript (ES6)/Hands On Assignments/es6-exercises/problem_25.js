// Topic: Then second argument for error
// Concept: `.then(onFulfilled, onRejected)` can handle errors (prefer `.catch`).
// Problem: Handle rejection via second arg to `then`.
// Hints: Demonstrate flow plus `finally`.
// Examples:
// Input: demoThenSecondArg()
// Output: logs handled + finally


function demoThenSecondArg() {
  // TODO: implement
}

// Test cases (uncomment to run after implementation)
// // demoThenSecondArg();
