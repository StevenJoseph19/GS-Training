// Topic: Destructuring (nested objects)
// Concept: Destructure nested properties into variables.
// Problem: Extract `department.address.city`.
// Hints: Pattern `{ department: { address: { city } } }`.
// Examples:
// Input: getCity(user)
// Output: 'Bangalore'


function getCity(user) {
  // TODO: implement
}

// Test cases (uncomment to run after implementation)
// // console.log(getCity({ department: { address: { city: "Bangalore" } } }));
